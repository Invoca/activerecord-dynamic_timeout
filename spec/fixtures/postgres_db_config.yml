test:
  encoding: utf8
  username: <%= ENV['DATABASE_MYSQL_USERNAME'] || '' %>
  password: <%= ENV['DATABASE_MYSQL_PASSWORD'] || '' %>
  adapter: postgresql
  pool: 5
  database: postgres_dynamic_timeout_test
  host: <%= ENV['DATABASE_MYSQL_HOST'] || 'localhost' %>
  variables:
    statement_timeout: 10000
